<!doctype html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS file -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">

    <link rel="stylesheet" href="{{asset('css/style.css')}}">
    <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>
<nav class="navbar navbar-expand-md navbar-light bg-light sticky-top">
    <div class="container-fluid">
        <a class="small-logo" href="#"><span>Influencers</span></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('index_route') }}">HOME</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('articles_route') }}">ARTICLES</a>
                </li>
                <li class="nav-item logo">
                    <a class="nav-link" href="{{ path('index_route') }}"><span>Influencers</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('new_article') }}">CREATE</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('authors-ranking') }}">RANKING</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
{% block content %}
    <div class="container">
        <div class="jumbotron index">
            <div class="index-container">
                <h3>WELCOME TO</h3>
                <h1>Influencers</h1>
                <p class="lead">Try your chance and become a trendsetter</p>
                <div class="buttons">
                    <a href="{{ path('articles_route') }}"><button type="button" class="btn btn-outline-light btn-lg">READ</button></a>
                    <a href="{{ path('new_article') }}"><button type="button" class="btn btn-outline-light btn-lg">CREATE</button></a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
<!-- Optional JavaScript -->
<!-- jQuery first, then Popper.js, then Bootstrap JS -->
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
{% block script %}
{% endblock %}
</body>

</html>